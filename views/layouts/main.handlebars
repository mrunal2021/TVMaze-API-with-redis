<!DOCTYPE html>
<html lang = en>
    <head>
        <meta charset="utf-8">
        <link rel="stylesheet" href="/public/site.css">
        <title>{{title}}</title>
    </head>
    <body>
        {{#if errorMessage}}
            <p class='error'>{{errorMessage}}</p>
        {{else}}
            <h1>TV Maze Shows</h1>
            <div>
                <form action = '/search' method = 'POST'>
                    <label for = 'searchBox'>
                        Enter Search Term
                        <input id = 'searchBox' type = 'text' name = 'searchTerm' placeholder = 'Search Term'>
                    </label>
                    <button class='searchButton' type='submit'>Search</button>
                </form>
            </div>
            {{{body}}}
        {{/if}}
    </body>
</html>